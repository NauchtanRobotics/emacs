;; (semantic-mode 1)

;; This is a minor mode which runs semantic-complete-analyze-inline-idle during
;; idle time. Instead of trying to complete the symbol immediately, it will just
;; display the possible completions, and underline the current symbol the cursor
;; is on.
;; (global-semantic-idle-completions-mode)
